# 2026-08-09 工作日志

## 接手 Open Matrix Publisher（全域矩阵）项目
- 通读 README / GITHUB_OPENSOURCE_STRATEGY / 执行手册 + 核心代码，确认项目背景与目标：
  开源 AI-Native 跨平台视频静默分发引擎，14+ 平台，MIT 免费，双轮驱动（Web UI + CLI），用作品传播 Martin·木齐科技 个人 IP。
- 建立优化任务清单（8 项，见 TaskList）：T1 SAU_ROOT 可配置 / T2 去产品硬编码 / T3 flask 依赖 / T4 index 部署化 / T5 提交推送 / T6 GitHub Pages / T7 国际平台 E2E / T8 示例脚本整理。

## 本轮已完成（Batch 1，安全重构，无发布动作）
- T1: `SAU_ROOT` 改为 `os.environ.get("SAU_ROOT", 原路径)`（real_uploader_engine / qr_login_manager / interactive_login）。
- T2: 剔除核心引擎中的产品/品牌硬编码词（real_uploader_engine 默认 tags、chat_ai_bridge 兜底文案、local_bridge_server 默认 video_file/topic/兜底文案）→ 改为通用占位。
- T3: requirements.txt 补 `flask>=3.0.0`（原缺，导致文档流程 Web UI 起不来，已验证系统 python3 无 flask）。
- T4: index.html 移除第656行硬编码本机视频绝对路径；将 localhost:5001 API 调用改为 `API_BASE` 变量（本地相对、Pages 下回退 localhost 桥接）。
- T5: 提交并 push 到 origin/main（fe6021e..3148a14，7 文件，+29/-15），确认无 cookies 入库。

## 待推进（需用户配合）
- T7: X/LinkedIn/TikTok/Instagram 端到端实测（需用户登录账号 + 测试视频）。
- T8: batch_dispatch_*/multi_platform_dispatcher_v2 产品脚本归入 examples/；评估恢复通义千问 AI 入口（README 提到通义但代码仅豆包/Kimi）。

## GitHub 连接确认（用户要求"连接到 GitHub"）
- 经 `git ls-remote` + GitHub API 确认：repo 已连且在线（public, Python, main, 我们 commit 3148a14 已上）。
- API 暴露缺口：license=null（README 称 MIT 但无 LICENSE 文件）、description/topics/homepage 均为空、has_pages=false。
- 已补 LICENSE(MIT) 并提交推送（3148a14..a4b0e5f）。
- WorkBuddy 内置 "GitHub" 连接器当前显示 disconnected —— 那是让我能在对话里管 issues/PR/release 的授权，需用户在 Settings→Connectors→GitHub 点 Trust 开启（我无法代为 OAuth）。连上后可用 API 设 description/topics/homepage 与启用 Pages。

## 2026-08-09 第二轮（用户连上 GitHub 连接器后）
- GitHub 连接器已 connected；用 gh CLI（账号 muqizb-tech-martin，含 repo/workflow scope）直接管仓库元数据。
- 设 description（中英双语简介）、homepage（Pages 地址）、10 个 topics（video-automation / social-media / headless / selenium / playwright / content-distribution / open-source / ai-native / multi-platform / youtube）。
- 启用 GitHub Pages（main 分支根目录），线上验证 HTTP 200，标题/`main-video`/`API_BASE` 均在；README 演示链接与线上地址一致。
- 结论：仓库在线元数据 + 静态部署全部补齐，开源曝光与可部署性到位。T6 完成。

## 2026-08-09 第三轮（UI 重构，用户指出木齐科技/生物材料/氢能等关键词与项目无关）
- 通读 index.html（1190 行）发现大量"富氢热灸贴/木齐科技/固态氢/生物材料"硬编码污染：<title>、头部标签、默认文件名+本机绝对路径、文案模板、确认弹窗、JS 默认值(tags/getCleanTopic/currentSelectedVideoPath) 全是产品词。
- 逻辑不合理处：第二个"分发模式"(bilingual) 点击无任何实际作用；视频大小写死 "42.9 MB"；确认弹窗不校验是否已选视频。
- 整体重写 index.html（223+/159-）：① 头部定位改为与分发引擎相关的标签（跨平台视频分发 / AI 原生引擎 / MIT 开源免费）；② 新增四步流程指示（选视频→写文案→选平台→一键分发，随操作高亮）；③ 模板改为通用（产品亮点/种草爆款/跨境英文），去除所有品牌词；④ 视频大小改为选中后动态计算；⑤ 确认弹窗增加"未选视频"拦截；⑥ AI 助手补回通义千问（README 早有提及，代码原缺）；⑦ 默认 tags 发 []，与后端 default 对齐。
- 校验：产品词 0 残留、无失效引用、<div> 69/69 闭合平衡。已推送 23d9285。Pages 将自动重建。
- 关键认知：Open Matrix Publisher 定位是"开源 AI 原生跨平台视频静默分发引擎"，UI/文案/默认值必须通用，不得混入木齐科技氢健康产品内容。

## 2026-08-09 第四轮（UI 视觉精修，"腾讯品味"级产品感）
- 用户要求继续"优化修改"UI。在第三轮结构重构基础上做视觉精修：
- 调色板收敛：原 neon 撞色（#00e6ff/#00ffc8/#ffd700 等纯高饱和）改为更克制协调的 --accent-cyan #38bdf8 / teal #2dd4bf / gold #eab308 / red #ef4444，统一把全文散落的 rgba(0,230,255)/(0,255,200)/(255,215,0) 字面量替换为新色值（保持一致不撞色）。
- 减弱发光：.glass-panel/.brand-logo-fusion/.btn-primary 的 glow 阴影大幅收敛；品牌 logo 渐变同步改为新色。
- 图标一致性：平台状态由 emoji（⏳/✅/🔑/🔒）改为 FontAwesome（fa-circle / fa-circle-check / fa-qrcode / fa-lock）；静态部署（Pages）未连本地引擎时，状态改为清晰的"未连接"（fa-plug-circle-xmark）。
- 新增 footer：MIT 开源声明 + 开源仓库/在线文档链接 + v1.0 版本号，收尾更完整。
- 校验：产品词/emoji 0 残留、<div> 71/71 闭合。已推送 90d43d1。

## 2026-08-09 第五轮（让项目真正能跑起来 · 实现国内+国际分发）
- 用户最终目标：让项目跑起来、实现国内8家+国际6家视频分发；做完自测到"发布标准"再叫他一起实测（明确禁止我擅自发布）。
- 审计结论：SAU(/Users/martin/social-auto-upload) 完整存在，含 `sau` CLI + 14 个平台 cookies（全部≥50字节，登录态齐全）+ Python3.12 venv(patchright/playwright 已装)。引擎两条路径( SAU CLI / 自定义上传器 )与 SAU 实际能力对得上。
- 修复的阻断性 bug（推送 dfb8f79）：
  1. real_uploader_engine 自定义平台原来用 `'''{desc}'''` 拼进执行代码 → 改为环境变量 OMP_PUBLISH_ARGS(JSON) 传递，消除注入/语法风险。
  2. tiktok_adapter.publish 签名 `(video,title,tags,schedule_hour=16)` 把引擎第4参 desc 误当排期小时且强制定时 → 改为 `(video,title,tags,desc=None)` 且 publish_date=0 立即发布。
  3. zhihu_uploader / toutiao_uploader 硬编码本机 cookie 路径 + headless=False（违反静默分发）→ 改为 SAU_ROOT 环境变量 + headless=True；weibo 同步去硬编码。
  4. base.py/__init__.py/tiktok_adapter 的 SAU_ROOT 硬编码改为读环境变量（与引擎一致，其它机器可用）。
  5. instagram_uploader 原恒返回 False → 改为真实尝试（新建→选文件→下一步→填文案→分享，成功跳转 /p/ 才算成功）。
  6. local_bridge_server 的 cookie_extractor 改为可选导入（缺 browser_cookie3 也不致控制台启动失败）。
  7. start_ui.sh 改为用 SAU venv python（含 flask/browser_cookie3/patchright/playwright），并自动补装依赖、支持 SAU_ROOT 覆盖。
- 运行环境补齐：SAU venv 原本无 pip/flask → ensurepip + 装 flask/browser_cookie3（现单 Python 即可跑控制台与上传）。
- 已验证（不实际发布）：8 个自定义上传器全部 import OK 且含 publish()；14/14 平台 check_profile_logged_in=True；桥接服务干净启动、/api/status 返回 14/14 logged_in(HTTP200)；sau CLI upload-video 参数与引擎命令完全匹配；Clash 代理 7890 在监听（国际平台可用）。
- 仍须用户实测（需真账号+真实发布）：各平台对活站点的实际上传成功率、Cookie 发布时新鲜度、IG 反自动化是否真能过。防重 Ledger(dispatch_history.json)+is_already_published 已就位，默认禁止重复发布。

## 用户进入实测阶段（19:37）
- 用户确认进入"尝试发一条视频"阶段。实测前自检：Clash 7890 在线、5001 空闲、start_ui.sh 可执行、14 家 Cookie 齐全。
- 已用 `./start_ui.sh`(后台 nohup) 启动桥接服务：/api/status 返回 running、14/14 logged_in、HTTP200，且 start_ui.sh 自动打开了用户浏览器页面。
- 给用户实测指引：浏览器开 http://localhost:5001 → 选测试视频→写文案(可选/可用AI)→勾平台(建议先国内1-2家)→一键分发；发完人工登录各平台后台确认(脚本成功≠已上线)；国际需 Clash(已在线)；防重绝不二传。

## 修复致命架构 bug（用户实测前发现，20:42）
- 现象：前端 `index.html` 选视频只拿 `file.name`（浏览器安全沙箱拿不到真实绝对路径），后端却把文件名在项目目录拼成绝对路径读文件 → 分发必失败（找不到文件）。
- 修法：新增 `/api/upload-video`（multipart 接收文件本体，存项目 uploads/ 返回绝对 saved_path）；前端 `startRealDispatch` 先上传文件再以其绝对路径分发；`start-upload-task` 加文件存在校验（不存在返回 400 绝不发布）。uploads/ 已加 .gitignore。
- 验证（不实际发布）：/api/upload-video 返回 saved_path 且文件落盘；start-upload-task 对不存在路径返回 400 file_not_found。已提交 106e87a..6349ba6。
- 运行注意：桥接服务必须用 Bash run_in_background（或 ./start_ui.sh）启动；在普通 Bash 里 nohup & 会在命令结束后被沙箱回收进程组。
