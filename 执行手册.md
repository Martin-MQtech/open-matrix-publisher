# 执行手册 — Open Matrix Publisher (全域矩阵)

> **本文件用途**：本手册是该项目的唯一权威参考文档。无论是人类接手还是任何 AI Agent（Codex、Claude Code、Google Antigravity、腾讯 Workbuddy 等）接管此项目，读完本文件即可获得完整背景、当前状态、技术架构与后续任务清单，无需依赖任何特定会话或聊天记录。

---

## ⛔ 执行铁律（接手项目的任何 Agent 必须在执行发布前逐条确认）

> 以下规范来自 2026-08-07 的真实事故复盘。违反任何一条，可能导致视频在平台上重复发布，且**无法通过脚本撤回**。

### 铁律一：发布前必须确认无任何分发进程在运行

收到发布指令后，**第一步**永远是 kill 所有可能残留的分发进程：

```bash
pkill -f dispatch.py
pkill -f batch_dispatch
pkill -f real_uploader_engine
pkill -f sau
# 确认输出为空，再继续
ps aux | grep -E 'dispatch|uploader|sau' | grep -v grep
```

只有确认输出为空，才可以启动新的分发任务。

### 铁律二：全程只允许运行唯一一条分发链路

- 严禁同时运行多个分发脚本
- 严禁同时启动多个 AI 子代理并行分发同一批平台
- 若使用子代理分工，每个平台只能分配给唯一一个子代理，绝对不得重叠
- 防重 Ledger（`dispatch_history.json`）只在单一脚本进程内有效，**绕过它直接调用上传函数则完全失效**

### 铁律三：脚本"成功"不等于视频已上线

- 脚本日志的「发布成功」只代表点击了平台的「提交」按钮
- 发布完成后，必须明确告知用户：**请登录各平台创作者后台，人工确认视频状态**（审核中 / 已发布 / 被拒）
- 不得将脚本输出当作发布成功的最终证明

### 铁律四：发现重复发布立即停止，不要用脚本修复

- 一旦发现重复提交，**立刻 kill 所有进程**，不要继续执行任何上传操作
- 由用户人工登录平台后台删除多余内容
- AI Agent 不得自行尝试撤稿（各平台撤稿行为不稳定，可能造成更多问题）

### 事故复盘与正式警告（2026-08-07 & 2026-08-08 持续更新）

> **【多平台自动化分发防重复与脚本生效警告】**
>
> 1. **防重机制**：在执行视频多平台自动化发布时，严禁同时启动多条分发路径。防重 Ledger（历史记录文件 `dispatch_history.json`）与锁文件（`.dispatch.lock`）在 `dispatch.py` 内部生效。已发布成功的平台会被防重锁（🔒）强制屏蔽。
> 2. **脚本加载路径**：`real_uploader_engine.py` 必须优先在 `sys.path[0]` 挂载本项目本地的 `custom_uploaders/` 目录，否则 Python 会误导入旧的 SAU 全局脚本。
> 3. **今日头条真正上传入口**：必须访问 `https://mp.toutiao.com/profile_v4/xigua/upload-video` 才能直接命中 `input[type=file]` 节点，访问其他 graphic/publish 路由会导致 0 个上传节点。
> 4. **微博上传完成轮询**：微博大视频切片需要严格轮询等待进度，才能触发点击「发布」。
> 5. **知乎 Cookie Token**：知乎鉴权核心为 `z_c0` Cookie，扫码登录时需等待该 Token 彻底写入后再关闭 context。

---

## 交付前审核机制（强制，2026-08-18 建立）

> **原则：任何改动交付给用户之前，必须全量自检——低级错误、逻辑不自洽、使用不通顺，一个都不放过。**
> 背景：桌面版曾因 `sys.executable` 在打包态指向 App 本体，导致「扫码登录」弹出第二个软件窗口——开发态一切正常所以长期未暴露。

**执行步骤（每次交付前必走）：**

1. **自动审计（机器查）**：
   ```bash
   python3 scripts/pre_delivery_audit.py            # 全量检查（12 类）
   python3 scripts/pre_delivery_audit.py --smoke    # 桌面版必加：对运行中服务冒烟
   ```
   覆盖：语法编译 / sys.executable 雷区 / __file__ 可写路径 / 打包产物完整性 / 前端资源 / 外网依赖 / API 接线 / 20 平台口径 / 登录覆盖 / 静态路由白名单 / 服务冒烟 / 文档口径 / TODO。
   **门禁：❌ 失败 = 阻断交付，必须修复后重跑；⚠️ 警告 = 逐条确认。**
2. **打包态冒烟（桌面版必做）**：启动打包产物 → `/api/health` 200 → 点「扫码登录」应弹出**真实浏览器**（不是第二个软件窗口）→ 静态资源 200。详见 `docs/DELIVERY_REVIEW.md`。
3. **人工确认（机器不可代）**：真实发布一次到平台后台确认、登录实测、视觉检查、重启后数据持久化。
4. CI 已自动门禁：`.github/workflows/pre-delivery-audit.yml` 在 push/PR 时自动跑审计。

---

## 一、项目背景、创始人信息与竞品深度调研

### 1.1 创始人与品牌战略

- **创始人**：Martin · MQ Tech（马丁）
- **GitHub**：[@Martin-MQtech](https://github.com/Martin-MQtech)
- **公司/品牌**：木齐科技 (MUQI Tech)，官网 [www.emuqi.com](https://www.emuqi.com)
- **核心定位**：跨境企业主 + 内容创作者 + 独立开发者，正在向 Builder & Influencer 双重身份深度演进
- **战略目标**：用开源作品传播个人 IP 与技术影响力，不以直接变现为唯一目标，通过高质量的开源工程与 Product-Led Growth (PLG) 积累 GitHub Stars、开发者社区认可与国际行业地位。

### 1.2 真实痛点起源

2026 年 8 月，Martin 在为木齐科技的**富氢热灸贴**制作并分发 4K 电影级双语视频时，面临着极高的运营摩擦：
- 同一条视频（中文版 + 英文版）需要人工分别登录抖音、快手、小红书、微博、B站、视频号、今日头条、百家号、番茄、知乎、YouTube、TikTok、X、LinkedIn、Facebook、Instagram 等 20 个平台；
- 每次分发需要重复填写标题、正文描述、Tag 标签，人工操作耗时 1~2 小时，极易出错且极其枯燥。

### 1.3 商业竞品深度剖析与开源减法原则

在立项初期，团队对国内现存的代表性多账号分发工具（如**蚁小二**、**小V猫**等）进行了深入调研：

| 维度 | 蚁小二 (Yi Xiao Er) | 小V猫 (Xiao V Mao) | **Open Matrix Publisher (全域矩阵)** |
|---|---|---|---|
| **商业模式** | 高额按月/按年订阅（59~数百元/月） | 基础免费 + 增值月卡诱导 (88元/月) | **100% 开源免费 (MIT License)** |
| **产品形态** | 臃肿的 Windows 客户端 | 传统桌面客户端 | **轻量 Web UI 控制台 + AI-Native 双引擎** |
| **隐私安全** | 账号凭证与 Cookie 需上传至商业云端 | 依赖厂商云端通道 | **100% 本地纯静默落盘 (`cookies/` 不离本机)** |
| **功能范围** | 塞入员工考勤、绩效考核、财务统计 | 繁琐的套路化发布链条 | **做减法：专注“视频无感极速分发”极致场景** |
| **AI 适配** | 无原生 AI Agent 接口 | 传统固化标题复制 | **AI-Native 原生集成，支持 Agent 零门槛接管** |

### 1.4 产品核心设计哲学（三法则）

1. **免费打底 (Zero-Cost & Free-First)**：
   - 坚持 100% 开源免费 (MIT License)，绝不设强制按月订阅门槛，绝不强制用户购买收费大模型 API 密钥。
   - 原生支持豆包、Kimi、通义千问、ChatGPT 等免费网页端 Chatbot 的提示词一键复制与剪贴板智能解析，保障任何普通创作者零成本上手。
2. **极致便捷的聚合集成中枢 (Convenience Hub)**：
   - 本工具的灵魂在于**“极致的连接与聚合”**——把分散在不同网页、电脑文件夹、账号背后的工作流收拢至统一中枢，以最少的点击次数完成最高效的分发。
3. **高阶智商弹性扩展 (Elastic API Ceiling)**：
   - 预留高阶 API 密钥插槽（支持 DeepSeek、OpenAI、Claude、Ollama），为追求全自动化的大批量企业用户与 AI Agent 提供无限智商天花板。

### 1.5 尖刀优先与克制演进战略 (Sharp Spearhead & Lean Extensibility)

- **当前唯一核心尖刀：视频矩阵 100% 稳定打通**：
  - 拒绝过早膨胀和功能堆叠，坚决不做臃肿大包。
  - 第一阶段集中所有工程力量，把**国内 10 大平台 + 海外 10 大平台（20 平台已接入）**的视频并发静默发布、Cookie 登录态保持与防封控机制做到极致扎实。
- **平台网络是动态生长的（2026-08-18 定稿）**：当前 20 平台只是起点，后续按"选择更好的渠道"持续增删调优——平台会不断增加、调整、也可能下线。
  - **平台清单是数据，不是身份**：新增/下线平台只改 4 处数据源（前端 `PLATFORMS` / 后端引擎清单 / `interactive_login` / `mcp_server`），审计 B4 强制四者一致；UI 计数（国内/海外/已登录）已全部动态化，不硬编码数量。
  - **品牌与 Slogan 不得绑定具体平台数**（"一条内容，多域分发"），平台数只作为事实数据出现在矩阵表。
- **架构预留模块化演进管道 (Future Satellites)**：
  - 在母体系统底层保持清晰的插拔式解耦，为未来平滑演进预留标准化接口：
    - 📱 **图文动态管道** (`drivers/post/`)：多图 + 短文案自适应发布（小红书/微博/X/Threads）；
    - 📄 **深度长文博客管道** (`drivers/article/`)：Markdown/富文本文章同步（公众号/知乎/Medium/Substack）；
    - 📚 **电子书与白皮书管道** (`drivers/ebook/`)：EPUB/PDF/MOBI 编译与数字出版（Amazon KDP/Google Books/微信读书）；
    - 🎙️ **多语声音频与播客管道** (`drivers/audio/`)：双语/多语种语音内容同步（Spotify/Apple Podcasts/喜马拉雅）。
  - 各类扩展以独立卫星驱动方式接入，绝不侵蚀核心视频引擎的轻量与极速。

### 1.6 平台接入矩阵 (Platform Access Matrix)

> **口径**：20 平台 = 国内 10（全部已接入）+ 海外 10（9 已接入 + 1 暂不支持）。
> **引擎分工**：`SAU 引擎`（social-auto-upload 的 `sau` CLI，成熟稳定）6 个；`自定义适配器`（`custom_uploaders/` 下独立模块）14 个。
> **登录**：控制台卡片点「扫码登录/刷新」→ 弹窗登录 → Cookie 自动写盘 `cookies/`（本地、`.gitignore` 屏蔽）。

#### 🇨🇳 国内阵列（10 平台 · 全部已接入）

| 平台 | id | 引擎 | 登录方式 | 验证状态 |
| :--- | :--- | :--- | :--- | :--- |
| 视频号 | tencent | SAU 引擎 | 微信扫码 | ✅ 已接入 |
| 抖音 | douyin | SAU 引擎 | 扫码 | ✅ 已接入 |
| B站 | bilibili | SAU 引擎 | 扫码 (biliup) | ✅ 已接入 |
| 快手 | kuaishou | SAU 引擎 | 扫码 | ✅ 已接入 |
| 小红书 | xiaohongshu | SAU 引擎 | 扫码 | ✅ 已接入 |
| 微博 | weibo | 自定义适配器 | 账号密码 | ✅ 已接入 |
| 今日头条 | toutiao | 自定义适配器 | 账号密码 | ✅ 已接入 |
| 知乎 | zhihu | 自定义适配器 | 扫码 | ✅ 已接入 |
| 百家号 | baijiahao | 自定义适配器 | 扫码 | ✅ 已接入 |
| 番茄视频 | fanqie | 自定义适配器 | 扫码 | ✅ 已接入 |

#### 🌏 海外阵列（10 平台 · 9 已接入 + 1 暂不支持）

| 平台 | id | 引擎 | 登录方式 | 验证状态 |
| :--- | :--- | :--- | :--- | :--- |
| YouTube | youtube | SAU 引擎 | Google 账号 | ✅ 已接入 |
| Facebook | facebook | 自定义适配器 | 账号密码 | ✅ E2E 实测发布成功 |
| TikTok | tiktok | 自定义适配器 | 扫码 / Google / Apple | ✅ E2E 实测发布成功 |
| Instagram | instagram | instagrapi 移动 API | 账号密码 | ✅ E2E 实测发布成功 |
| X (Twitter) | x | 自定义适配器 | 账号密码 / Google | ✅ E2E 实测发布成功 |
| Dev.to | devto | 免费官方 API | API Key（控制台配置） | ✅ 免费 API，实测发布成功 |
| WordPress | wordpress | 免费官方 API | 应用密码（控制台配置） | ✅ 免费 API，请求链路已验证 · 支持媒体库真视频上传 + 文章嵌入 |
| Telegram | telegram | 免费 Bot API | bot_token + chat_id（控制台配置） | ✅ 免费 API，请求链路已验证（支持真实视频上传） |
| Pinterest | pinterest | 免费官方 API | access_token + board_id（控制台配置） | ✅ 免费 API 已接入（视频 Pin 需 Standard access 审核，待真实验证） |
| LinkedIn | linkedin | —（待官方 API） | — | ⛔ 暂不支持（Web 反自动化拦截，参照 Postiz 走官方 Marketing API） |

> ¹ 2026-08-18 实测：X / TikTok / Facebook / Instagram（instagrapi 移动 API）/ Dev.to（免费 API）端到端发布成功；LinkedIn 网页发布流程拦截自动化操作，已按「不硬磕、复用开源方案」原则降级为暂不支持，待官方 API 通道（Postiz 等开源方案已走通该路径）；Pinterest 免费 API 适配器已接入，待 OAuth Standard access 审核后真实验证。

> ✅ **2026-08-18 已补齐**：百家号 / 番茄视频已加入 `interactive_login.PLATFORMS`（含登录 URL / auth_cookies / cookie_files），控制台「扫码登录」可直接一键调起浏览器；上传适配器此前已就绪。

---

## 二、双轮驱动产品架构共识 (Dual-Engine Architecture)

对于真正的用户与系统而言，单独的命令行工具或单独的网页都不足以构成完整闭环。本项目坚持 **“双轮驱动”** 的产品共识：

```text
                              ┌──────────────────────────────────────────────┐
                              │  Open Matrix Publisher (全域矩阵双轮驱动)      │
                              └──────────────────────┬───────────────────────┘
                                                     │
                   ┌─────────────────────────────────┴─────────────────────────────────┐
                   ▼                                                                   ▼
    【轮次一：人类 Web UI 可视化控制台】                                  【轮次二：AI-Native / CLI 自动化引擎】
   · 线上 URL: https://martin-mqtech.github.io/open-matrix-publisher/  · 接入入口: dispatch.py / real_uploader_engine.py
   · 本地入口: index.html / ./start_ui.sh (Port 5001)                 · 服务人群: AI Agent (Claude/ChatGPT/Antigravity)、Cron
   · 服务人群: 运营人员、创作者、互联网访问用户                            · 核心体验: 零前台干预、无头静默分发、.dispatch.lock 进程锁、
   · 核心体验: 20 平台网格卡片、拖拽点选视频、                              dispatch_history.json 历史 Ledger 硬阻断
     一键 [📷 扫码登录/刷新] 调起、🔒 已发布防重金框                       
```

1. **人类可视化控制台 (`index.html`)**：
   - **GitHub Pages 静态界面预览页**：访问 `https://martin-mqtech.github.io/open-matrix-publisher/` 可预览 UI（真实分发能力需本地运行 `./start_ui.sh`，视频与 Cookie 始终留在本机）；
   - 解决用户“需要一个直观界面来点选账号登录、浏览选择本地视频文件、预览文案”的体验刚需；
   - 提供 20 大平台中英双语控制卡片，点击 `[扫码登录/刷新]` 即可自动在 Mac 桌面弹窗引导用户手机 App 扫码。
2. **AI-Native 自动化引擎 (`dispatch.py`)**：
   - 解决 AI Agent 接管时的零门槛调用，任何 AI 语言模型只需一行命令即可全权代理完成分发。

---

## 三、技术架构与 100% 实证避坑规则

### 3.1 四阶混合驱动架构 (Hybrid Drivers)

| 驱动优先级 | 方案名称 | 适用平台 | 特性与优越性 |
|---|---|---|---|
| **Tier 1 (首选)** | **social-auto-upload (SAU)** | 抖音、快手、小红书、B站、视频号、YouTube | 引擎原生切片上传，B站支持 pseudo-TTY 伪终端 |
| **Tier 2** | **Patchright 反检测 Chromium** | 微博、今日头条、知乎、Facebook、TikTok | 深度屏蔽 WebDriver 标识，绕过平台风控检测 |
| **Tier 3** | **CDP 直连 Chrome** | 需要复用真实浏览器 Session 的平台 | 无缝复用 Mac 本地 Chrome 已登录 Cookie |
| **Tier 4 (兜底)** | **官方 OAuth API** | YouTube Data API v3, LinkedIn API | 终极稳定性保障 |

### 3.2 实战避坑秘籍（2026-08-08 验证归纳）

1. **今日头条上传入口路径**：
   - ❌ 误区：访问 `mp.toutiao.com/profile_v4/graphic/publish-video`（为图文/小视频混合入口，会导致 0 个上传 Input 节点）。
   - ✅ 正解：必须直达西瓜视频极速上传端 `https://mp.toutiao.com/profile_v4/xigua/upload-video`。
2. **微博大文件切片上传等待**：
   - ❌ 误区：触发文件 `set_input_files` 后立即查找点击「发布」按钮（会导致虚报成功，实际视频未传完）。
   - ✅ 正解：必须增加 100% 轮询等待逻辑，确认视频上传切片彻底落盘后才触发最终提交。
3. **知乎鉴权 Token 保护**：
   - ❌ 误区：仅判断 URL 离开登录页就关闭上下文。
   - ✅ 正解：知乎的核心鉴权 Token 为 `z_c0` Cookie，扫码登录时需等待 `z_c0` 彻底写入 storage state 后方可退出。
4. **Python 包导入优先级**：
   - `real_uploader_engine.py` 必须在 `sys.path[0]` 显式挂载本项目本地的 `custom_uploaders/`，防止导入宿主机上旧版 SAU 的同名适配器。

---

### 3.3 核心依赖

```
social-auto-upload (SAU)   —  安装路径: 默认 ~/social-auto-upload/（自动检测，可用 SAU_ROOT=<路径> 显式指定）
                               虚拟环境: ~/social-auto-upload/.venv/
                               CLI命令:  .venv/bin/sau（Windows: .venv/Scripts/sau.exe）
                               Cookie目录: ~/social-auto-upload/cookies/
                               首次缺失时 ./start_ui.sh 自动检测并引导一键安装（Windows: start_ui.bat）
                               跨平台路径统一由 omp_paths.py 解析（Windows Scripts / POSIX bin）

Playwright                 —  headless Chromium 自动化
Patchright                 —  Playwright fork，增强反检测能力
Python 3.10+
```

### 3.4 防重复发布机制（幂等控制）

每次发布前，引擎检查 `dispatch_history.json`：
- 若该平台 + 该视频文件已有 `status: success` 记录 → **强制跳过**，输出警告
- 无论成功失败，发布结果都写入历史文件，保证可追溯

### 3.5 Cookie 管理规范

- 所有平台 Cookie 存放于 `cookies/` 目录（本地，`.gitignore` 已屏蔽）
- SAU 的 Cookie 单独存放于 `~/social-auto-upload/cookies/`（即 `$SAU_ROOT/cookies/`）
- Cookie 命名格式：`{platform}_default.json`
- **永远不要将 `cookies/` 目录提交到 Git**

### 3.6 已知技术坑与解法

**微博发布页 DOM 问题：**
```python
# 微博输入框用 CSS 隐藏，Playwright 的 fill() 会超时
# 正确做法：JS 事件注入
page.evaluate("""
    const el = Array.from(document.querySelectorAll('input, textarea'))
               .find(i => i.placeholder && i.placeholder.includes('标题'));
    if (el) {
        el.value = '你的标题';
        el.dispatchEvent(new Event('input', { bubbles: true }));
        el.dispatchEvent(new Event('change', { bubbles: true }));
    }
""")
```

**微信视频号登录 Cookie 未写盘：**
- 必须等待页面跳转至 `https://channels.weixin.qq.com/platform` 后
- 显式调用 `storage_state(path=...)` 写盘
- 之后才能关闭浏览器 context

**TikTok `schedule_hour` 类型错误：**
```python
# tiktok_adapter.py 中必须做 int 转换
hour_val = int(schedule_hour) if str(schedule_hour).isdigit() else 16
publish_date = publish_date.replace(hour=hour_val, ...)
```

**X (Twitter) 媒体上传检测：**
- `MediaUploadProgress` 元素可能不出现（视频很小时）
- 应先检查元素是否存在，不存在则直接等待预览图出现

---

## 四、目录结构说明

```
open-matrix-publisher/  （本目录，即工具的独立家目录）
│
├── 执行手册.md                      ← 本文件，项目总纲
├── README.md                        ← GitHub 公开展示页（中英双语）
├── requirements.txt                 ← pip 依赖列表
├── .gitignore                       ← 安全屏蔽 cookies/ 等敏感文件
│
├── real_uploader_engine.py          ← 核心引擎（SAU + 自定义上传器的统一调度层）
├── qr_login_manager.py              ← 首次扫码登录并保存 Cookie
├── cookie_extractor.py              ← 从本地 Chrome 导入已有 Cookie
├── local_bridge_server.py           ← 本地 HTTP 桥接服务（供 Web 控制台调用）
├── multi_platform_dispatcher_v2.py  ← 高层调度器（带队列、重试、日志）
│
├── batch_dispatch_domestic.py       ← 快速入口：一键跑国内平台（含完整文案）
├── batch_dispatch_global.py         ← 快速入口：一键跑国际平台（英文版）
│
├── open_matrix_publisher/           ← Python 包
│   ├── __init__.py
│   └── core.py
│
├── custom_uploaders/                ← 每个平台的独立上传适配器
│   ├── base.py                      ← 基类，定义统一接口
│   ├── tiktok_adapter.py
│   ├── x_uploader.py
│   ├── linkedin_uploader.py
│   ├── facebook_uploader.py
│   ├── instagram_uploader.py
│   ├── weibo_uploader.py
│   ├── zhihu_uploader.py
│   └── toutiao_uploader.py
│
├── cookies/                         ← 本地 Cookie 存储（Git 忽略，不可提交）
│   └── .gitkeep
│
├── examples/
│   └── my_campaign.py               ← 模板：为新项目/新视频写分发脚本的起点
│
├── platform_credentials.template.json  ← 凭证模板（无真实数据，可提交）
│
└── docs/                            ← 策略与架构文档
    ├── ADVANCED_AUTOMATION_ARCHITECTURE.md
    ├── BENCHMARK_AND_RESEARCH.md
    ├── BUILDER_INFLUENCER_BRANDING.md
    ├── CREATIVE_PROMOTION_POSTS.md
    ├── MULTI_PLATFORM_DISPATCH_GUIDE.md
    ├── OBJECTIVE_CROSS_VERIFICATION.md
    ├── OPENSOURCE_GROWTH_STRATEGY.md
    ├── OPENSOURCE_PRODUCT_PLAN.md
    ├── PROMOTION_POSTS.md
    └── SECURITY_AND_PRIVACY_GUIDE.md
```

---

## 五、与其他目录的关系

### 5.1 原始项目目录（保留，不删）

```
/Users/martin/Desktop/Codex视频处理/20260806 富氢热灸贴双语视频/
```

这是**木齐科技富氢热灸贴产品**的第一个实际使用案例。该目录保留：
- 原始视频文件（中文版、英文版 .mp4）
- 素材图片、HTML 宣传页
- 该产品专用的 `batch_dispatch_domestic.py` / `batch_dispatch_global.py`（含硬编码文案和路径）
- 该目录连接至 GitHub 仓库（`.git` 存在）

**本工具目录（此处）与原始项目目录的关系：**
- 本目录 = **工具本体**，通用、可复用、不含任何具体产品信息
- 原始目录 = **工具的第一个使用案例**，包含特定产品的文案、视频

### 5.2 social-auto-upload (SAU) 依赖

```
/Users/martin/social-auto-upload/
```

这是第三方开源项目，提供抖音/快手/小红书/B站/视频号/YouTube 的原生自动化上传能力。本项目的 `real_uploader_engine.py` 封装并调用它。

**安装定位逻辑（2026-08-18 起）**：`start_ui.sh` 按优先级定位 SAU：① 环境变量 `SAU_ROOT` → ② 常见路径自动检测（`~/social-auto-upload` 等）→ ③ 交互终端引导一键安装；运行 `./start_ui.sh --check` 可只查看检测结果。**Windows 对应实现为 `start_ui.ps1` + `start_ui.bat`**（双击即可，逻辑等价：检测 `%USERPROFILE%\social-auto-upload`、引导安装、`-Check` 检测模式）。

**注意**：SAU 目录不属于本项目，不需要迁移或修改其代码。

### 5.3 GitHub 仓库

```
https://github.com/Martin-MQtech/open-matrix-publisher
```

- 仓库名：`open-matrix-publisher`
- 命名规范：GitHub/URL/技术场合全小写（`martin-mqtech`）；宣传/品牌场合用大写（`Martin-MQtech`）
- 本地 Git 已初始化，remote 已配置，**尚未执行第一次 push**

---

## 六、安全规范（任何 Agent 必须遵守）

1. **Cookie 文件绝对不能提交 Git**：`cookies/` 目录已在 `.gitignore` 中，任何时候都不要用 `git add cookies/` 或强制提交
2. **不能硬编码真实凭证**：源代码中不得出现任何真实的 API Key、密码、Cookie 字符串
3. **不能将用户数据上传至任何云端**：所有操作在本地完成
4. **修改 `.gitignore` 需谨慎**：不得删除对 `cookies/`、`.env`、`platform_credentials.json` 的屏蔽规则
5. **视频文件不入库**：`*.mp4` 已被 `.gitignore` 屏蔽，不得修改

---

## 七、当前状态快照（2026-08-08 最新实证更新）

### 已完成 (100% 验证通过)
- [x] **工具核心引擎** `real_uploader_engine.py` 优化完成（修复了 `sys.path[0]` 导入优先级）
- [x] **微信视频号**（Tencent Channels）100% 成功发布
- [x] **抖音**（Douyin）100% 成功发布
- [x] **B站**（Bilibili）100% 成功发布 (pseudo-TTY 模式)
- [x] **快手**（Kuaishou）100% 成功发布
- [x] **微博**（Weibo）100% 成功发布（内置大文件切片上传等待）
- [x] **今日头条**（Toutiao）100% 成功发布（已挂载真正西瓜视频上传地址 `profile_v4/xigua/upload-video`）
- [x] **知乎**（Zhihu）100% 成功扫码并捕获 `z_c0` 鉴权 Token，实现实证端到端无缝发布
- [x] **Facebook 国际版** 100% 成功发布
- [x] **Web 可视化控制台 Dashboard** (`index.html`) 与后端 Flask 服务 (`local_bridge_server.py`) 100% 开发构建完毕
- [x] **多重防重锁机制** (`.dispatch.lock` 进程锁 + `dispatch_history.json` 历史硬锁 + 预检报告) 完成
- [x] **扫码登录助手** (`interactive_login.py`) 成功集成 Playwright 双向降级容错机制
- [x] **GitHub 远程同步**（`https://github.com/Martin-MQtech/open-matrix-publisher` 100% 实时 Push）

### 待完成（Action Items）
- [ ] **国际平台（X、LinkedIn、TikTok、Instagram）**：进一步验证端到端测试与真实视频分发
- [ ] 开启 GitHub Pages 全球在线公网域名绑定（`https://martin-mqtech.github.io/open-matrix-publisher/`）

---

## 八、新 Agent 接手时的行动清单

如果你是新接手该项目的 AI Agent，请按以下顺序执行：

### Step 1：了解环境
```bash
# 确认工具目录结构
ls ~/Documents/2026\ BUSINESS\ MTRIX\ /20260807\ OpenMatrixpublister/

# 确认 SAU 依赖可用（路径以实际 SAU_ROOT 为准，如 ~/social-auto-upload）
~/social-auto-upload/.venv/bin/sau --help

# 确认 Python 版本
python3 --version

# 确认 Playwright 安装
python3 -c "from playwright.sync_api import sync_playwright; print('OK')"
```

### Step 2：查看当前发布历史
```bash
cat /Users/martin/Desktop/Codex视频处理/20260806\ 富氢热灸贴双语视频/dispatch_history.json
```

### Step 3：检查 Cookie 状态
```bash
ls ~/social-auto-upload/cookies/
# 检查每个 *_default.json 是否存在且大于 10 字节
```

### Step 4：执行首次 Git Push（如尚未完成）
```bash
cd ~/Documents/2026\ BUSINESS\ MTRIX\ /20260807\ OpenMatrixpublister/
git add .
git status   # 确认没有 cookies/ 或 credentials 文件被追踪
git commit -m "feat: initial standalone release — Open Matrix Publisher v1.0"
git push -u origin main
```

### Step 5：根据待完成任务继续推进
参考本文件第七节的"待完成"清单，按优先级逐项推进。

---

## 九、多视频项目的调用架构（核心设计）

### 9.1 设计原则

> **工具只有一份，项目可以无限多。**

每做一个新视频，**不需要复制任何引擎代码**，只需在该视频项目目录下创建一个 `campaign.json` 配置文件，然后调用工具目录下的统一入口 `dispatch.py` 即可。

```
                    ┌─────────────────────────────────┐
                    │   Open Matrix Publisher (工具)    │
                    │   ~/Documents/.../OpenMatrix...  │
                    │   ├── dispatch.py  ← 统一入口    │
                    │   ├── real_uploader_engine.py    │
                    │   ├── custom_uploaders/          │
                    │   └── cookies/  (共享登录凭证)   │
                    └───────────────┬─────────────────┘
                                    │ 被调用
              ┌─────────────────────┼──────────────────────┐
              ▼                     ▼                      ▼
   视频项目 A               视频项目 B              视频项目 C
   富氢热灸贴               新产品 X               品牌宣传片
   └── campaign.json        └── campaign.json      └── campaign.json
       └── video_cn.mp4         └── video.mp4          └── video_en.mp4
```

**Cookie 只需登录一次**，所有项目共享同一套凭证，无需重复扫码。

### 9.2 新视频项目的标准操作流程

**每次制作完新视频，只需 3 步完成分发：**

**第 1 步**：在视频项目文件夹内复制模板
```bash
cp ~/Documents/2026\ BUSINESS\ MTRIX\ /20260807\ OpenMatrixpublister/examples/campaign_template.json \
   /path/to/your/video/project/campaign.json
```

**第 2 步**：编辑 `campaign.json`，填入视频路径、标题、描述、标签
```json
{
  "campaign_name": "富氢热灸贴_2026Q3",
  "tasks": [
    {
      "video": "./富氢热灸贴_中文版.mp4",
      "title": "你的视频标题",
      "desc": "视频描述",
      "tags": ["标签1", "标签2"],
      "platforms": ["douyin", "xiaohongshu", "weibo"]
    }
  ]
}
```

**第 3 步**：一行命令分发
```bash
python3 ~/Documents/2026\ BUSINESS\ MTRIX\ /20260807\ OpenMatrixpublister/dispatch.py \
        --config /path/to/your/video/project/campaign.json
```

支持参数：
```bash
# 只发特定平台
python3 .../dispatch.py --config ./campaign.json --platforms douyin xiaohongshu

# 演练模式（不实际发布，检查配置）
python3 .../dispatch.py --config ./campaign.json --dry-run

# 查看所有支持的平台
python3 .../dispatch.py --list-platforms
```

### 9.3 文件组织建议

```
~/Documents/2026 BUSINESS MTRIX /
│
├── 20260807 OpenMatrixpublister/     ← 工具本体（唯一，永久维护）
│   ├── dispatch.py                   ← 统一分发入口
│   ├── real_uploader_engine.py
│   ├── cookies/                      ← 所有平台登录凭证（共享）
│   └── ...
│
├── 20260806 富氢热灸贴/               ← 视频项目 A（只含内容，不含引擎）
│   ├── campaign.json                 ← 该活动的分发配置
│   ├── 富氢热灸贴_中文版.mp4
│   ├── 富氢热灸贴_英文版.mp4
│   └── dispatch_history.json         ← 该活动的发布记录（自动生成）
│
├── 20261001 新品发布/                 ← 视频项目 B（同样的结构）
│   ├── campaign.json
│   ├── video_cn.mp4
│   └── dispatch_history.json
│
└── 20261201 年度品牌片/               ← 视频项目 C
    ├── campaign.json
    └── ...
```

**每个视频项目目录只需要：**
- 视频文件本身
- `campaign.json`（告诉工具：发哪个视频、发到哪些平台、用什么文案）
- `dispatch_history.json`（自动生成，记录已发状态，防止重复发布）

### 9.4 Cookie 首次登录（只做一次）

第一次使用新平台前，运行：
```bash
cd ~/Documents/2026\ BUSINESS\ MTRIX\ /20260807\ OpenMatrixpublister/
python3 qr_login_manager.py
```
扫码登录后，凭证保存在 `cookies/` 目录，**所有后续视频项目共用**，不需要重复登录。

Cookie 失效时（通常数周至数月），重新运行上面的命令刷新对应平台即可。

---

## 十、品牌与 IP 战略要点

Martin 明确表达的方向（新 Agent 需理解并贯彻）：

1. **开源不以直接变现为目的**，以影响力积累为核心 KPI（GitHub Stars、社区关注、行业认可）
2. **做减法原则**：拒绝臃肿，专注"视频无感静默分发"这一个核心场景做到极致
3. **AI-Native 优先**：工具的设计要让任何 AI Agent 都能直接接管和使用，这是产品差异化的核心
4. **个人 ID 传播**：每一个提交、每一个文档、每一个宣传帖都要体现 Martin-MQtech 的 ID
5. **交互风格**：Martin 本人偏好客观、理性、专业、干练的沟通风格，不需要吹捧和情绪化修饰

### 品牌命名约定
| 场景 | 用法 |
|---|---|
| GitHub URL / npm / pip / 技术标识符 | `martin-mqtech`（全小写） |
| 对外宣传 / 品牌展示 / 社交媒体 | `Martin-MQtech`（大小写混合） |
| 项目中文名 | 全域矩阵 |
| 项目英文名 | Open Matrix Publisher |

---

## 十、参考文档索引

| 文件 | 内容 |
|---|---|
| `docs/MULTI_PLATFORM_DISPATCH_GUIDE.md` | 平台适配现状、Cookie 避坑指南、故障排查路线 |
| `docs/ADVANCED_AUTOMATION_ARCHITECTURE.md` | 无感后台分发技术架构（四阶驱动） |
| `docs/SECURITY_AND_PRIVACY_GUIDE.md` | 凭证安全与隐私保护规范 |
| `docs/BENCHMARK_AND_RESEARCH.md` | 竞品分析（蚁小二、小V猫）与增长渠道研究 |
| `docs/OPENSOURCE_PRODUCT_PLAN.md` | 跨平台战略（macOS → Windows/Linux） |
| `docs/BUILDER_INFLUENCER_BRANDING.md` | 个人 IP 打造战略 |
| `docs/OPENSOURCE_GROWTH_STRATEGY.md` | GitHub 开源增长策略 |
| `docs/PROMOTION_POSTS.md` | 社交媒体发布文案草稿 |
| `examples/my_campaign.py` | 新活动脚本模板 |
| `docs/DESIGN_AND_PRODUCT_PLAN.md` | **产品形态、视觉设计、账号/CRM 与分阶段路线图（2026-08-18 定稿）** |
| `docs/RELEASE_CHECKLIST.md` | **开源发布 REPO 验收清单（push 前敏感文件/素材/口径逐项核对）** |
| `docs/DELIVERY_REVIEW.md` | **交付前审核机制（自动审计 + 打包态冒烟 + 人工确认，强制门禁）** |
| `scripts/pre_delivery_audit.py` | **交付前全量审计脚本（12 类检查，FAIL 阻断交付）** |

---

## 十一、产品与视觉设计方案（2026-08-18 定稿）

> 完整方案见 `docs/DESIGN_AND_PRODUCT_PLAN.md`，本章为强制摘要，作为后续接手 Agent 的指导方针。

### 11.1 结论
- **Slogan（2026-08-18 定稿）：一条内容，多域分发** —— 定位锚点是「一条内容」（不限视频/图文/文章）与「多域分发」（跨平台/跨渠道/跨地域，不限平台数量）。**禁止用具体平台数量（如"20 平台"）作品牌口号**，平台数只作为事实数据出现在矩阵表中。
- 产品形态：**Web 优先**（唯一交付物），桌面 DMG 为后续套壳。
- 视觉：**黑/灰 + 橙黄**，扁平线条，去赛博朋克（去渐变/光晕/霓虹）。
- 平台图标：改用 **Simple Icons 平面单色官方 logo**，弃用 FontAwesome 彩色方块。
- 账号：**暂不做注册/登录，整体后置**；产品形态为开源、可下载、本地运行、免注册。
- 节奏：**先收尾 Phase 1（视频分发），再做 Phase 2（文章/图文）**；账号/CRM 后置。

### 11.2 视觉系统（强制）
- 色板：`--bg #15130f`（暖黑）｜`--surface #1c1a15/#23201a`（炭灰）｜`--ink #f2eee6`｜`--muted #9a9384`｜`--accent #e8892e`（橙）｜`--accent-2 #f3b25a`（黄）。
- 字体：标题 Fraunces + Noto Serif SC；正文 Inter + Noto Sans SC。
- 质感：发丝边框、中性低对比阴影；**渐变仅允许主 CTA 一处**。
- Logo：大 M 作背景（Matrix / MUQI 首字母，暖白粗体几何）+ 中心橙黄渐变 O（Open）+ 最外一圈细环（"Open Matrix"），炭黑圆角底，32px 仍清晰可辨。核心设计点：O 的圆洞把 M 中间的 V 形套出、V 完整可见。渐变仅限 O 一处，M 与外圈保持纯色。资产：`logo.svg`（网页/favicon）、`logo.png`（1024px，README/桌面）、`logo-mono-{white,black,orange}.svg/.png`（单色版，深底/浅底/强调）、`favicon.ico`（16~256px 多尺寸）、`icon.icns`（macOS 打包）、`icon.ico`（Windows 打包）。完整使用规则（深底/浅底/单色/最小尺寸/留白/禁用）见 `docs/DESIGN_AND_PRODUCT_PLAN.md` §2.4。

### 11.3 平台图标（强制）
- Simple Icons 平面单色 SVG，**本地自托管**，18px、官方品牌色、不套彩色底框；深色品牌色反白；覆盖不到的国内平台用统一扁平字标兜底。

### 11.4 账号与 CRM（已后置）
- **暂不做用户注册/登录**：开源下载、本地运行、免注册可用；注册/云同步/CRM 全部后置，不在近期排期。
- 若将来需要，再按设计文档 §4 的三档复杂度方案另行启动（BaaS 起步，不自研 auth）。

### 11.5 分阶段路线图
- Phase 1（收尾）：Simple Icons 图标 + 4 个国际平台验证 + 文案口径统一（20 平台）+ 内部自测清单。
- Phase 2：文章/图文分发 + 分发记录增强（失败重试/手动标记）。
- Phase 3：macOS DMG 封装 + 团队/多账号 + 高级 API。

### 11.6 执行约定
- 改动 UI 前对照设计文档 §2/§3；涉账号/数据库/云/合规先改方案再写码；UI 不得虚标平台数量（当前口径：20 平台已接入）；防重/单链路/人工确认铁律不变。

### 11.7 复用优先（Reuse-First）
- 任何新模块先评估开源社区是否已有成熟实现，优先复用/封装；复用成本更低则严禁自研。
- 已确认复用：social-auto-upload（上传）、Patchright/Playwright（浏览器）、Simple Icons（平台图标）、Google Fonts + Font Awesome（字体/UI 图标）、**instagrapi（Instagram 移动 API，实测发布成功）**。
- **平台接入策略（2026-08-18 定稿）**：平台反自动化不无限硬磕——优先开源方案/官方 API；别人已解决的直接采用；暂时无解的降级为「暂不支持」置灰标记，不阻塞主线。LinkedIn 已降级（待官方 Marketing API，参照 Postiz），X/Facebook/TikTok/Instagram 已实测发布成功。
- **平台扩展路线（2026-08-18 调研，详见 `docs/POSTIZ_INTEGRATION_RESEARCH.md`）**：Postiz 32 平台清单已验证官方 API 路径——**不引入 Postiz 本体**（AGPL + 重型架构冲突），按清单逐个接入轻量适配器：**只接免费 API 平台**（Dev.to/Hashnode/WordPress/Telegram 等 API-key 类），**不接收费 API**（X/Reddit——X 已有 Cookie 自动化免费方案）。国内 10 平台继续走 Cookie 路线（Postiz 没有国内平台，这是护城河）。
- Phase 2 前需调研：文章/图文发布适配器、视频封面（FFmpeg）、MCP 官方 SDK。（账号 BaaS / CRM 已随注册后置，暂不调研。）

### 11.8 双模式使用（产品理念，2026-08-18 定稿）
- **模式 A（独立软件）**：人类在控制台手动选内容/勾平台/一键发布。
- **模式 B（AI Agent 接管）**：用户把项目文件夹/工具箱交给 AI Agent，下指令后后台全自动分发——本产品所有接口（REST / CLI / MCP）开发之初即由 AI Agent 驱动，天然支持；可能比模式 A 更流行。
- **架构强制**：任何 UI 改动不得破坏接口层；新增平台必须同步暴露到引擎 / interactive_login / MCP 清单，确保 Agent 也能驱动。

---

*最后更新：2026-08-18 | 维护者：Martin-MQtech*
